from numpy import *
arr = array([2,3,4.0,5,6],int) #here we have specified the type of data,though the original array is float but we have specified it to be int.
print(arr)
print(arr.dtype) #it prints the datatype